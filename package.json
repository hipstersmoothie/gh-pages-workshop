{
  "name": "cra-gh-pages",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://hipstersmoothie.github.io/gh-pages-workshop/",
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "push-dir --dir=build --branch=gh-pages --cleanup"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "resolutions": {
    "babel-jest": "24.7.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
